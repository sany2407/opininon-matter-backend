steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', '$_IMAGE_NAME', '.']

images:
  - '$_IMAGE_NAME'

substitutions:
  _IMAGE_NAME: 'asia-south1-docker.pkg.dev/${PROJECT_ID}/cloud-run-source-deploy/om-backend:${COMMIT_SHA}'
